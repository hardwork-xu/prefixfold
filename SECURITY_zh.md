# 安全与限制

[English](SECURITY.md)

我维护文档定义的本地 CPU 算子范围。PrefixFold 不提供多租户服务、沙箱、密码学缓存或生产大模型运行时。

- 仅接受有限 FP32 数组与支持的形状，算术溢出会报错。调用者负责前缀语义完全一致、位置编码、内存预算，以及调用期间查询数组不被外部修改。
- 后缀容量固定。注意力临时空间参数约束保守的显式数组估算，不涵盖 Python、NumPy、BLAS 或进程 RSS。
- NPZ 输入禁用 pickle，并检查声明的解压大小，默认上限 512 MiB。请使用可信文件：这不是针对恶意文件头或拒绝服务的完整防护。CLI 拒绝覆盖输入或已有输出。
- 批次锁串行化更新与注意力计算；独立进程和外部数组修改不受该锁约束。程序不创建持久缓存或网络监听服务。

敏感问题优先使用仓库已启用的私密漏洞报告渠道；如未启用，可提交最小问题以请求私下协调，不公开漏洞细节或个人数据。此处不提供私人联系地址。
